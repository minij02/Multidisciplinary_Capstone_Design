# ----------------------------------------------------------------------
# 1. Backend: Spring Boot / Gradle Files
# ----------------------------------------------------------------------
# Gradle build directory
/build/
/bin/
/.gradle/

# Gradle wrapper logs
hs_err_pid*.log
replay_pid*.log

# ----------------------------------------------------------------------
# 2. Backend: Security / Local Secrets (MUST BE IGNORED)
# ----------------------------------------------------------------------
# 실제 키 값이 담긴 로컬 설정 파일 (Git에 절대 커밋되면 안 됨)
/src/main/resources/application-local.properties

# ----------------------------------------------------------------------
# 3. Frontend: Node.js / React Files (Trip 폴더 내부)
# ----------------------------------------------------------------------
# 의존성 폴더
/trip/node_modules
# Vite 빌드 아티팩트 (일반적으로 dist 폴더)
/trip/dist

# npm lock 파일
package-lock.json
npm-debug.log*

# ----------------------------------------------------------------------
# 4. IDE Files (VS Code / IntelliJ)
# ----------------------------------------------------------------------
.idea/
.vscode/
*.iml